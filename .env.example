# ZeroDeploy Configuration Template
# Copy this file to .env and customize as needed

# Environment Configuration
ENVIRONMENT=production

# Domain Configuration (Optional)
# Leave empty to use system defaults
# Or specify a custom domain suffix (e.g., mydomain.local, example.com)
DOMAIN_SUFFIX=

# Port Configuration
# Leave empty for automatic port selection (recommended)
# Or specify a specific port (e.g., 8080, 3000, etc.)
ZERO_DEPLOY_PORT=

# Application Settings
DEBUG=false

# Docker Settings
COMPOSE_PROJECT_NAME=zerodeploy

# Optional: Custom DNS settings
# DNS_SERVER=8.8.8.8

# Optional: SSL configuration (for nginx profile)
# SSL_CERT_PATH=./ssl/cert.pem
# SSL_KEY_PATH=./ssl/key.pem

# Advanced Docker settings
# COMPOSE_FILE=docker-compose.yml
# COMPOSE_PROJECT_NAME=zerodeploy